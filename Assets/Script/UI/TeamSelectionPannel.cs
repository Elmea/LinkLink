using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TeamSelectionPannel : MonoBehaviour
{
    [SerializeField] public Transform[] m_team1SlotsPos;
    [SerializeField] public Transform[] m_team2SlotsPos;
    [SerializeField] public Transform[] m_middleSlotsPos;

    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
